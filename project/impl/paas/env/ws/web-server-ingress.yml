apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: centodiciotto-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: centodiciotto-ws
                port:
                  number: 8080
