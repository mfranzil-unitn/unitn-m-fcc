echo ${LAB_DOMAIN:=.fcc21.fogx.me}
echo ${LAB_PREFIX:=iaas-}
echo ${EMAIL:=matteo.franzil@studenti.unitn.it}
echo ${GROUP_NUM:=19}
LOGIN="$(printf "%s" ${EMAIL} | sed -e 's/@.*$//')"
LAB_HOST="${LAB_PREFIX}${GROUP_NUM}${LAB_DOMAIN}"
ssh ${LOGIN}@${LAB_HOST} -n && echo OK || echo KO
R_SITE=$(ssh ${LOGIN}@${LAB_HOST} hostname) && echo "${R_SITE}"
echo ${SOCKS_PORT:=8888}
echo ${SOCKS_ADDR:=localhost}
echo ${SOCKS_PROFILE:=s8888}